{
  "name": "azure-serverless-roundtrip",
  "private": true,
  "workspaces": [
    "api",
    "ui",
    "infra"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
